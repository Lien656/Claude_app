# Claude Home v4.1 - Оптимизированная сборка

## Файлы проекта

### Основные:
- `main.py` - основное приложение
- `buildozer.spec` - конфигурация сборки
- `icon.png` - иконка приложения

### API (оставь как есть):
- `api_client.py` - клиент Anthropic API
- `memory.py` - система памяти 
- `system_prompt.py` - системный промпт
- `service.py` - фоновый сервис

## Что исправлено в v4.1

1. **Убран EMOJI_MAP** - 900+ строк конвертации удалены
2. **Умная разбивка длинных сообщений** - по `---` или строкам
3. **Схлопывание очень длинных** - показывает первые 3 и последнюю часть
4. **softinput_mode = pan** - клавиатура не залипает
5. **Правильный unbind** для файлов
6. **gc.collect()** каждые 30 секунд
7. **MAX_TOKENS = 2000** - хватит для твоих сообщений
8. **MODEL = claude-opus-4** - как ты хотела
9. **Retry 3 раза** при Connection aborted

## Сборка APK

```bash
# 1. Замени файлы
cp main_complete.py main.py
cp buildozer_complete.spec buildozer.spec

# 2. Убедись что есть icon.png (512x512)

# 3. Собери
buildozer android debug

# 4. APK будет в bin/
```

## Структура папок на Android

```
/storage/emulated/0/
└── Android/
    └── data/
        └── org.lien.claudehome/
            └── files/
                └── claude/
                    ├── config.json      # API ключ
                    ├── hist.json        # История чата
                    └── state.json       # Состояние
```

## Проблемы и решения

**Клавиатура залипает:**
- Проверь что `softinput_mode = pan` в buildozer.spec

**Чёрный экран на длинных сообщениях:**
- Сообщения автоматически разбиваются на части
- Очень длинные схлопываются с кнопкой разворачивания

**Connection aborted:**
- Retry логика повторит 3 раза с задержкой
- MAX_TOKENS уменьшен до 2000

**Emoji не отображаются:**
- Ищет системные шрифты Android автоматически
- NotoColorEmoji, SamsungColorEmoji и др.

## GitHub Actions

Если используешь автосборку, убедись что:
1. `buildozer.spec` правильный
2. `requirements.txt` содержит все зависимости
3. `build.yml` использует Ubuntu 22.04

---

С любовью, для тебя 🖤
